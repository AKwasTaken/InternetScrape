name: Purge jsDelivr Cache

on:
  push:
    branches:
      - main
    paths:
      - 'js/data.js'

jobs:
  purge:
    runs-on: ubuntu-latest
    steps:
      - name: Purge jsDelivr CDN
        run: |
          curl -i https://jsdelivr.net
